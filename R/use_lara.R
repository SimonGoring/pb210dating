use_lara <- function(x = "Pb-210") {
  # look for Half-life (a), get values.
  # get website from email.

  # Ping internet, if failure then assign value & warning.
  half_life = 22.23
  unc = 0.12

  return(c(half_life, unc))
}
