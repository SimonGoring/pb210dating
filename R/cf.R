cf <- function(total_col,
               support_col,
               sample_date = NULL,
               constants = NULL,
               n_sect = length(s_mass),
               constant = NULL,
               inv_top = 0,
               inv_bot = 0){

}
